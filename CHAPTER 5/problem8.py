# Can you change the values inside a list which is contained in set S.print
# s = {8, 7, 12, "Rahul", [1,2]}
# LETS DO THIS ->>

s = {8, 7, 12, "Rahul", [1,2]}
print(s)

# POINT : We can not include list in a set because set requires all the element to be immutable and hashable.
